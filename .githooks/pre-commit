#!/usr/bin/env bash
set -euo pipefail

PY="$(command -v python3 || command -v python || true)"
if [ -z "$PY" ]; then
  echo "pre-commit: python3/python not found on PATH" >&2
  exit 1
fi
exec "$PY" scripts/pre_commit_checks.py
