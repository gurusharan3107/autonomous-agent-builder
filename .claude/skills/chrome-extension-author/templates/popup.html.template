<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <title>REPLACE-NAME</title>
  <style>
    body { width: 280px; margin: 0; padding: 12px 14px;
           font: 13px/1.4 -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
           color: #1a1a1a; background: #fff; }
    h1 { font-size: 14px; margin: 0 0 6px; }
    p  { margin: 0 0 8px; color: #555; }
    button { width: 100%; padding: 8px; border: 0; border-radius: 6px;
             background: #3b82f6; color: #fff; font-weight: 600; cursor: pointer; }
    button:hover { background: #2563eb; }
    .status { margin-top: 8px; font-size: 12px; color: #6b7280; }
  </style>
</head>
<body>
  <h1>REPLACE-NAME</h1>
  <p>REPLACE — short description visible when the operator clicks the icon.</p>
  <button id="primary">REPLACE-ACTION-LABEL</button>
  <div class="status" id="status"></div>
  <script src="popup.js"></script>
</body>
</html>
