{
  "skill_name": "builder-test",
  "evals": [
    {
      "id": 1,
      "prompt": "I just fixed agent_chat_result_publisher.py to remove the hardcoded 'Ready for Builder to start now' string. Test my change.",
      "expected_output": "Skill activates and runs all 6 phases. Phase 1 confirms the bad string is absent from publisher. Phase 2 passes intent classifier assertions. Phase 4 verifies ask_user_question event is emitted. Verdict table produced with PASS/WARN/FAIL per phase.",
      "assertions": [
        "Phase 0 runs and confirms builder is on :9876 with devpulse project loaded",
        "Phase 1 grep for 'Ready for Builder to start now' in publisher returns no matches",
        "Phase 2 runs all 8 intent classifier assertions and reports pass/fail per case",
        "Phase 5 produces a PASS/WARN/FAIL table with at least one row per phase"
      ]
    },
    {
      "id": 2,
      "prompt": "run builder tests — just the unit phase",
      "expected_output": "Skill activates. Phase 0 (preconditions) runs first, then Phase 2 (unit) only. Intent classifier assertions, tool registry check, AskUserQuestion presence, feature_captured pattern all verified. Single-phase verdict emitted.",
      "assertions": [
        "Phase 0 runs before Phase 2 (preconditions always gate)",
        "Phase 2 runs intent classifier against at least 8 labelled cases",
        "Phase 2 checks for mcp__builder__backlog_item_update in tool registry",
        "Phases 1, 3, 4 are skipped",
        "Verdict table covers Phase 0 and Phase 2 only"
      ]
    },
    {
      "id": 3,
      "prompt": "verify the fix works",
      "expected_output": "Skill activates (trigger phrase matched). Asks operator which change to test via AskUserQuestion OR runs full loop if context is sufficient. Does not silently skip Phase 0 even if builder appears running.",
      "assertions": [
        "Skill activates — 'verify the fix works' is a listed trigger phrase",
        "Phase 0 runs and health-checks :9876 before any other phase",
        "If builder is NOT running, skill starts it from /home/gurusharangupta/Builder-Workspace/devpulse (not source repo)",
        "Skill does not declare PASS without producing a verdict table"
      ]
    }
  ]
}
