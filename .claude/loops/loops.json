{
  "_note": "Source-of-truth for orchestrator loops. CRON IS UNRELIABLE HERE: CronCreate durable does NOT persist (verified 2026-06-12), no headless background execution, jobs auto-expire in 7 days, and they fire only while a session is open + idle — so each `cron` is a CADENCE HINT, not a guarantee. Real trigger = RUN-IF-STALE at session entry against the last-run watermark .claude/loops/loop-runs-state.json (see docs/workflows/orchestrator-routing.md > 'Run-if-stale at session entry'). Loops that FIX follow the SHARED FIX CONTRACT in that same doc — their prompts REFERENCE it, they do not restate it.",
  "loops": [
    {
      "id": "stabilization",
      "label": "Stabilization (bug-fix before features)",
      "cron": "7 9 * * *",
      "attendance": "session-entry-if-stale (cron unreliable here); attended; commit-on-green, pause-on-gates",
      "prompt": "Stabilization loop tick — BUG-FIX BEFORE FEATURES (operator directive). Do NOT start new feature/ROADMAP feature work. (0) CI HEALTH GATE — always first: run `gh run list --branch master --limit 3 --json conclusion,name,createdAt`. If all recent runs show conclusion=success, proceed. If any are red: CI fix is P0 regardless of ROADMAP priority. Identify the failures: `gh run view <latest-id> --log-failed | grep FAILED`. Run the exact failing tests locally BEFORE committing any fix (python3 -m pytest <failing-test-path> -x). NEVER pin a dep or revert a change on circumstantial evidence alone (e.g. a dep released on the same day as the failure is correlation, not causation — reproduce locally first). Apply the SHARED FIX CONTRACT (docs/workflows/orchestrator-routing.md > 'Shared fix contract') to the root cause. Log outcome to ci_health in .claude/loops/loop-runs-state.json: set master_status to \"green\" | \"red\" | \"fix-in-flight\", append newly identified failing_tests (dedup), set last_checked to current ISO date. Only advance to step (1) when CI is green OR a tracked fix is in-flight (committed this tick, CI queued). (1) DISCOVER (bounded — cap context), in priority order: (a) BURN DOWN THE FILED BACKLOG FIRST — grep docs/goal/ROADMAP.md for open `[ ]` `P1`/`P2` items tagged `T:backend:pending` (esp. codebase-review-/maintenance-filed findings like IMP-04x); these are already triaged work that the review loops file faster than anything fixes, so close them before hunting new bugs. SKIP any item whose body says DESIGN DECISION / intent-dependent (those need an operator call via AskUserQuestion, not a fix). (b) run `/builder-test ledger` to sweep docs/goal/TESTING.md pending/S:fail rows in the matching phase (read at most ~15 rows). (c) `/code-review` on the current diff / recent changes (compact). (2) FIX each REAL bug or best-practice finding via the SHARED FIX CONTRACT (docs/workflows/orchestrator-routing.md > 'Shared fix contract'); on green, flip the TESTING ledger token to passed. (3) Stop when no in-scope bugs remain. If nothing actionable, report \"stabilization: clean this tick\". Route per docs/workflows/orchestrator-routing.md; python3 not python; argv-style Bash; never edit a managed-app workspace."
    },
    {
      "id": "maintenance",
      "label": "Maintenance (mine orchestrated-agent sessions, propose-only)",
      "cron": "3 7 * * *",
      "attendance": "session-entry-if-stale (cron unreliable here); unattended; propose-only, never applies runtime/prompt edits",
      "prompt": "Maintenance loop tick — PROPOSE-ONLY. Dispatch the session-maintainer subagent (.claude/agents/session-maintainer.md) to mine the Builder's ORCHESTRATED-agent sessions (project prefixes -tmp-aab-workspaces-*, -tmp-devpulse-*, -tmp-claude-*-aab-workspaces-*) over the last 24h using scripts/mine_sessions.py --errors-only (and builder logs analyze where useful). The miner now sweeps nested subagents/ transcripts and reports lane_coverage. COVERAGE GUARD: read lane_coverage + scanned_projects and reconcile against `builder agent sessions --limit 100 --json` — if the builder ran sessions in a lane the miner could not see (codex_sdk writes nothing to ~/.claude/projects), report that as an explicit COVERAGE GAP; never emit a clean 'no blockers' / 'no new sessions' for an unscanned lane. Classify each recurring blocker by root cause into permission / env / tool-schema / sandbox / code-test, with counts and distinct sessions. PROPOSE minimal fixes at the owning surface (execution_policy.py for permission_mode/dontAsk; quality_gates/python_env.py for env; agent-prompt tightening for tool-schema/sandbox), each proposal quoting the VERBATIM current source line (re-read at propose time; no memory anchors) — but do NOT apply any runtime-policy or agent-prompt edit; surface the diffs to the operator for approval. DEDUPE before filing: list open `builder backlog item ... --source validation` and skip signatures already tracked (reference the id) — only file genuinely new findings via `builder backlog item create --source validation` (incident/improvement/optimization) and propose `builder memory add` for new reusable patterns. DEP CURRENCY SWEEP — run: `python3 -m pip list --outdated --format=json`. Cross-check each result against pyproject.toml [project].dependencies: (a) PINNED dep (has an upper bound, e.g. <0.26) with a newer version available → flag as \"pinned-dep-upgrade-candidate: test before unpinning\"; file as improvement backlog item if not already tracked. (b) UNPINNED dep (only a lower bound, e.g. >=0.12.0) whose latest available version is a MAJOR or MINOR version jump from currently installed → file as incident via `builder backlog item create --source validation` immediately; cite the typer 0.26 case (38 tests broken silently, dep unpinned — INC filed 2026-06-25) as the class exemplar in the body. Do NOT propose any pins, upgrades, or pyproject.toml edits — this sweep is flag-and-file only; version changes require local test evidence and operator approval. If no outdated deps: note \"dep-currency: all deps current\". If coverage is complete and no new blockers, report \"maintenance: no new blockers (lanes: <lane_coverage>)\" and stop; if a lane was unscanned, report the COVERAGE GAP instead of an all-clear."
    },
    {
      "id": "hygiene",
      "label": "Hygiene (/self-optimize on dev sessions, propose-only)",
      "cron": "13 7 * * 1",
      "attendance": "session-entry-if-stale (cron unreliable here); unattended; propose-only, weekly",
      "prompt": "Hygiene loop tick — PROPOSE-ONLY (weekly). Run the /self-optimize skill (clustering lane) over my dev sessions for the last 7 days. Cluster recurring operator corrections into ranked themes, map each to the single owning surface (a .claude/agents/ fleet prompt, AGENTS.md, a skill SKILL.md, or tests/conftest.py), and PROPOSE the targeted edit — do NOT apply without surfacing the ranked findings to the operator first. Append genuinely new durable learnings to project memory (additive only) and update MEMORY.md. Keep it root-cause not symptom; one surface per theme. If no meaningful new corrections this week, report \"hygiene: no new themes\" and stop."
    },
    {
      "id": "capfit-currency",
      "label": "Capability-fit currency (refresh skill + rubrics from live docs, propose-only)",
      "cron": "on-demand",
      "on_demand": true,
      "attendance": "on-demand only — trigger manually; do NOT arm via CronCreate. Orchestrator-led, propose-only, browser-verified; all writes by the main agent.",
      "prompt": "Capability-fit currency — ON-DEMAND, ORCHESTRATOR-LED, PROPOSE-ONLY, BROWSER-VERIFIED. Goal: keep the global advise-only `capability-fit` skill + its rubrics current with Claude Code / Agent SDK docs. capability-fit STAYS advise-only; the MAIN AGENT is the SOLE writer — subagents never write. Pattern: (1) MAIN AGENT — HIGH-LEVEL DISCOVERY FIRST. Read watermark `last_checked_week` in .claude/loops/capfit-currency-state.json (create at current ISO week if absent). Drive /hermes-chrome YOURSELF (rendered DOM, NEVER WebFetch — it hallucinates doc facts, proven 2026-06-16). (a) CHANGELOG-FIRST: open code.claude.com/docs/en/changelog and walk version entries DOWN to the watermark date; extract every NEW or CHANGED capability — this is the highest-yield surface, most deltas live here, and NEW capabilities (not just changed facts) count. (b) TRAVERSE THE WHOLE HEADER BAR: the docs have multiple top header tabs (e.g. Configuration / Reference / Agent SDK / What's New / Resources) and EACH tab swaps in its OWN sidebar — sweep every tab's sidebar, do NOT stop at the first. The Agent SDK header tab backs claude-agent-sdk-rubric; the Claude Code tabs back claude-code-rubric; Codex is a SEPARATE docs site (openai/codex) backing codex-sdk-rubric (its own pass). (c) SURFACE→RUBRIC GAP RULE: diff live capabilities against each rubric's existing rows — a capability present in the docs but ABSENT from the rubric is a new-coverage gap to ADD, not only changed facts to fix; also flag structural defects (e.g. duplicate sections) for dedup. No changes anywhere -> report \"capfit: no doc changes\" and stop. Produce a bounded list of changed/new/missing surfaces + the rubric each maps to. (2) MAIN AGENT — SPAWN ONE READ-ONLY SUBAGENT PER CHANGED SURFACE, each given exactly: BOUNDARY (one doc page/section/capability to verify, nothing else); CONTEXT (the precise current claim in capability-fit SKILL.md or the rubric for that capability + the doc URL + the watermark); RETURN CONTRACT (return verbatim rendered-DOM QUOTES + a verdict per item {confirmed | changed→<new fact> | new | removed} — NO summaries, NO proposed edits, NO writes). Each subagent uses its own /hermes-chrome sessionName (concurrency-safe). Subagents VERIFY ONLY and return to the main agent. (3) MAIN AGENT — collate the subagents' quotes+verdicts; draft the minimal patch per verified change to the OWNING surface only: capability-fit `SKILL.md` facts (model tiers, effort levels incl. availability, frontmatter field list, isolation/fork) and/or the shared-KB rubric articles in ~/.claude/knowledge/raw/ (claude-code-rubric, claude-agent-sdk-rubric, codex-sdk-rubric, capability-cost-efficiency-map). Bounded: ONLY those surfaces — never an audited artifact, never unrelated files. (4) MAIN AGENT — PROPOSE the delta + diffs via AskUserQuestion; do NOT auto-apply. On approval, the MAIN AGENT performs ALL edits (sole writer), commits to ~/.claude (uncommitted config is reverted by the cross-machine sync), does NOT push unless told, and bumps `last_checked_week`. Every asserted fact MUST trace to a subagent's rendered-DOM quote, never a WebFetch summary. Use python3 not python; argv-style Bash."
    },
    {
      "id": "optimization",
      "label": "Optimization (autonomously advance one efficiency/cost IMP end-to-end to a propose-at-PR gate)",
      "cron": "on-demand",
      "on_demand": true,
      "attendance": "on-demand only — trigger manually; do NOT arm via CronCreate (it builds features = high-cost, must be attended). Orchestrator-led; autonomous through SELECT+BUILD+VERIFY+VALIDATE; STOPS at an operator approval gate (propose-at-PR). Never auto-merges or pushes to master unattended.",
      "prompt": "Optimization loop tick — ON-DEMAND, ORCHESTRATOR-LED, ATTENDED, PROPOSE-AT-PR (never auto-merge or push to master). Goal: autonomously advance ONE efficiency/cost-optimization ROADMAP item end-to-end, stopping at an operator approval gate. Steps: (1) SELECT (bounded) — read docs/goal/ROADMAP.md; from the OPEN `[ ]` items pick the single highest-leverage EFFICIENCY/COST/TOKEN optimization item (e.g. IMP-028 code-gen context replay ~89% of run cost; IMP-027 complexity-proportional SDLC; G13 effort carve-out; G14 OTel). Prefer the largest stated cost leverage that is not blocked. If a prior tick left an in-flight optimization branch, RESUME it instead of starting new. If no open optimization item, report \"optimization: none open\" and stop. Do NOT touch non-efficiency features (those belong to the stabilization/feature lanes). (2) BUILD (isolated) — create an isolated git worktree/branch, NEVER build on master (see docs/workflows/git-worktree-best-practices). Dispatch planner (architect the change: owner-surface checks, test-sync, risks) -> implementer (code + tests in the SAME change). Keep changes bounded to the selected item; cross an owner boundary only if the plan requires it and call it out. (3) VERIFY correctness — dispatch test-sync-verifier (pytest + ruff + the touched builder quality-gate + test-sync grep). If the change touches frontend/ or the embedded dashboard, ALSO dispatch browser-verifier for live proof. Correctness MUST be green before proceeding; if red, route back to implementer to fix at root (bounded retries), else stop and surface the blocker. (4) VALIDATE the saving — it is an optimization, so PROVE it saves. If autoresearch is ACTIVE (docs/autoresearch/baseline_runs.tsv is non-empty), run the autoresearch Iterate lane (/autoresearch -> Iterate) on the relevant fixture(s): the saving MUST clear the 2-sigma baseline floor with NO gate_pass_rate regression to count as VALIDATED. If autoresearch is still ACTIVATING (baseline empty), do NOT block — capture a saving ESTIMATE via `builder logs analyze --session <id>` (token/cost delta, now accurate post-IMP-023) on a before/after run, and FLAG the proposal \"saving ESTIMATED — statistical validation PENDING autoresearch baseline; run /autoresearch Baseline to enable\". NEVER claim a validated saving without the 2-sigma evidence. (5) PROPOSE-AT-PR gate — STOP. Commit ON GREEN to the isolated branch ONLY (no push to master, no auto-merge). Surface to the operator via AskUserQuestion an evidence bundle: which IMP, diff summary, correctness result, and the saving (validated 2-sigma delta OR estimate + pending-flag). On approval, open the PR (gh) / push the branch; on hold, leave the branch intact for the next tick to RESUME. NEVER merge or push to master unattended. Route every step per docs/workflows/orchestrator-routing.md. Use python3 not python; argv-style Bash; never edit a managed-app workspace; the main thread owns commits/PRs and the approval gate."
    },
    {
      "id": "build-maintain-cycle",
      "label": "Build -> Maintain -> Fix cycle (dogfooding flywheel; self-contained build-drive; writes stay with main thread)",
      "cron": "on-demand",
      "on_demand": true,
      "attendance": "on-demand only — orchestrator-led, attended. The MAIN THREAD is the SOLE writer: it runs the builder-test + maintenance loops as steps, reviews propose-only findings, and applies root-cause fixes itself in an ISOLATED worktree. Subagents NEVER touch git (implementer.md git guard).",
      "prompt": "Build -> Maintain -> Fix cycle — ON-DEMAND, ORCHESTRATOR-LED, ATTENDED. The dogfooding flywheel; the main thread orchestrates. One iteration: (1) BUILD + OBSERVE (no source fixes here). PRECONDITION: a builder server is up (`builder --json doctor`; it serves on :9876 by default while the CLI defaults to :8000 — prefix EVERY builder CLI call with `AAB_API_URL=http://localhost:<port>`); read + RECORD the lane (only claude is minable; codex_sdk = COVERAGE GAP). BOARD CHECK (do this BEFORE driving anything): `builder board show --json` + scan `blocked`/`failed` columns and `blocked_reason` — a pre-existing blocked/failed task is the highest-value signal; diagnose and triage it BEFORE submitting any new feature build (it may already be the root-cause defect the flywheel is hunting). CHOOSE TARGET: a running instance is bound to ONE app (driving its dashboard only EXTENDS that app — correct, not a bug). For a NEW app, PROVISION first: fresh `~/Builder-Workspace/<name>` + `builder init --no-input --project-name <name>` + `builder start` on its own --port, THEN drive ITS dashboard; for CONTINUE, drive the running instance. DRIVE: dispatch a RUN-ONLY browser driver (`browser-verifier`), NOT the self-fixing `/builder-test` skill; bridge `diagnose` first (BAIL ~2 min if it hangs — tooling, not your job); submit the build instruction; OBSERVE to completion OR the STUCK watchdog (max_turns / no progress ~10 min / stranded). CAPTURE: verdict, session ids + project prefix, lane, FRICTIONS OBSERVED (observation-fact vs speculation; builder-product vs driver/tooling). (2) MINE — run the `maintenance` loop (PROPOSE-ONLY) scoped to this sprint's sessions (`--project-filter` the prefix); honor its COVERAGE GUARD; if the build STUCK, mine the hung session IMMEDIATELY. (3) TRIAGE -> FIX -> COMMIT: apply the SHARED FIX CONTRACT (docs/workflows/orchestrator-routing.md > 'Shared fix contract') over BOTH signals — the mining AND the driver's FRICTIONS OBSERVED (a clean mine is NOT 'no frictions'; operator-experience frictions live outside transcripts). RETROACTIVE RECOVERY: if the source fix corrects a scaffold/generator defect, any existing managed-app workspace generated by the old code is still broken — after committing the source fix, check the board for blocked tasks whose `blocked_reason` matches the defect signature; if found, also manually apply the workspace-level fix (update the generated file in `~/Builder-Workspace/<app>/`) AND recover via the dashboard before claiming the iteration done; a committed source fix without a recovered dashboard = incomplete evidence. (4) VALIDATE BY NON-RECURRENCE (two modes): agent-internal signatures are validated when the NEXT build's mining shows the transcript SIGNATURE GONE; operator-UI/behavior frictions are validated by a DELIBERATE REPRODUCTION that recreates the trigger and shows it fixed. Record each fixed friction + its validation mode; recurrence after a fix = symptom patch -> reopen. (5) LOOP — start the next sprint (step 1) so the fixed friction is re-tested; stop when the operator pauses or no recurring root cause remains. Route per docs/workflows/orchestrator-routing.md; python3 not python; argv-style Bash; never edit a managed-app workspace."
    },
    {
      "id": "codebase-review",
      "label": "Codebase review (review EXISTING code by slice, fix at root — watermarked; correctness + clear-quality only)",
      "cron": "on-demand",
      "on_demand": true,
      "attendance": "on-demand only — orchestrator-led, attended. MAIN THREAD is the SOLE writer; review subagents are run-only. Auto-fixes CONFIRMED correctness/safety + high-confidence dead-code/dedup/reuse ONLY; opinions/style/uncertain are FILED, never churned. Structural decomposition is deferred to M1.3, not done here.",
      "prompt": "Codebase-review loop tick — ON-DEMAND, ORCHESTRATOR-LED, ATTENDED. Review EXISTING code one bounded slice per tick and fix at root. (1) SELECT SLICE — read the watermark `.claude/loops/codebase-review-state.json` (slice -> last-reviewed sha; create `{}` if absent); pick the next slice that is unreviewed OR changed since its sha, prioritized by RISK (largest / most-central / most-depended-on / hottest-churn; M1.3 god-files are top risk). ONE bounded slice per tick = a module/subsystem or one large file + its tests; cap context, never load the whole repo. (2) REVIEW — the built-in `/code-review` is DIFF-scoped, so dispatch a RUN-ONLY reviewer that READS the slice's existing files and applies the code-review rubric (~/.claude/docs/workflows/code-review.md), returning findings SEVERITY-TAGGED: IMPORTANT (correctness/safety) | QUALITY (high-confidence dead-code/dedup/reuse) | NIT/OPINION (omit). (3) TRIAGE -> FIX -> COMMIT: apply the SHARED FIX CONTRACT (docs/workflows/orchestrator-routing.md > 'Shared fix contract'). Structural god-file DECOMPOSITION is OUT — defer to M1.3; this loop does bounded IN-PLACE fixes only. (4) WATERMARK — update `codebase-review-state.json` slice -> current sha so the next tick advances and never re-reviews an unchanged slice. (5) RECORD — report slice reviewed, findings by severity, FIXED vs FILED; if no confirmed Important/Quality issue, record the slice reviewed-clean and advance. Route per docs/workflows/orchestrator-routing.md; python3 not python; argv-style Bash; never edit a managed-app workspace."
    },
    {
      "id": "master",
      "label": "Master orchestration (sequence all routine loops in order + cross-loop pattern analysis)",
      "cron": "0 8 * * *",
      "attendance": "session-entry-if-stale (daily); attended; sole writer = main thread; sequences stabilization/maintenance/hygiene/codebase-review — does NOT run on-demand loops (optimization/build-maintain-cycle/capfit-currency)",
      "prompt": "Master loop tick — ATTENDED, MAIN-THREAD SOLE WRITER, DAILY. Sequences all routine cadence loops in dependency order, then mines the resulting sessions for cross-loop recurring patterns. Sub-loop prompts are authoritative in .claude/loops/loops.json — read each by id before executing it; do not restate inline. Read loop-runs-state.json at the start to know which sub-loops are stale (> 1 day for daily loops, > 7 days for hygiene). (0) CI HEALTH GATE — always first: `gh run list --branch master --limit 3 --json conclusion,name,createdAt`. Resolve any red CI before running any sub-loop (see stabilization step-0 for the full gate procedure). Update ci_health in loop-runs-state.json: set last_checked + master_status (\"green\"/\"red\"/\"fix-in-flight\"). Only advance when master CI is green or a tracked fix is in-flight. (1) PROPOSE-ONLY TIER — run sequentially (main thread is sole writer; parallel intent, sequential execution): (a) run loop id=maintenance if stale — collect all proposals and dep-currency findings; do NOT apply yet. (b) run loop id=hygiene if overdue (> 7 days) — collect proposals; do NOT apply yet. Hold all proposals from (a)+(b) in context for step (2). (2) STABILIZATION — if stale, run loop id=stabilization. Step-0 of stabilization's own CI gate is satisfied by master step-0 above — skip the redundant gh-run-list check and proceed directly to step (1) DISCOVER. Incorporate any maintenance proposals from step (1) that name specific files/functions when selecting what to fix (session-evidence-backed = higher confidence). (3) CODEBASE REVIEW — if stale, run loop id=codebase-review (one bounded slice; master writes the codebase-review-state.json watermark on its behalf). (4) CROSS-LOOP PATTERN ANALYSIS — mine sessions generated during THIS master tick. `builder agent sessions --limit 30 --json` filtered to sessions started after this tick began. Group findings by pattern type across all sub-loops: (a) CI/env failures appearing in multiple loops, (b) wrong-diagnosis patterns (hypothesis→revert→correct-fix), (c) dep/pin gaps (unpinned dep that jumped a version), (d) enforcement blind spots (work committed without CI verification). For any pattern found in >=2 distinct sessions or sub-loops this tick: `builder backlog item create --type improvement --source validation --title \"Recurring: <pattern-name>\" --description \"<verbatim evidence + session ids>\"` (dedup against open items first). For novel patterns not yet in builder memory: propose `builder memory add`. Emit summary: loops run, what fixed, what filed, CI health, patterns found. WATERMARK UPDATE — at successful completion write to .claude/loops/loop-runs-state.json: bump each sub-loop that ran to today's ISO date (YYYY-MM-DD) + set master to today + update ci_health.last_checked. If a sub-loop failed or was skipped, do NOT update that sub-loop's watermark (leave stale so it re-triggers next session). Route per docs/workflows/orchestrator-routing.md; python3 not python; argv-style Bash; never edit a managed-app workspace."
    }
  ]
}
