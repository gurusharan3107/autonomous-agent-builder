# Development Scripts

## Dashboard Startup

`builder start` owns dashboard publication. It builds `frontend/`, copies the
output into `.agent-builder/dashboard/`, and launches the local dashboard/API
on the requested port.
